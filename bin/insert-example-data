#!/usr/bin/env python

from flaskheroku import init_webapp
from flaskheroku.model import db, Messages


def main():
  init_webapp()
  m = Messages("Hello, world!")
  db.session.add(m)
  db.session.commit()


if __name__ == '__main__':
  main()
